<!-- Ahora esta sera la pag secret -->

<h1>Fake Bitly</h1>
<h1><%= @message %></h1>
<p>Estas son tanto tus urls <%= session[:user_name] %> de bitly: </p>
<p>
  <% @url_all.each do |link|%>
   <!-- <p><%= link.url_nueva %></p> -->
    <a href="/send_url/<%=link.url_nueva%>" ><%= link.url_nueva %></a>    <!--  te da un link con la nueva url y notemos que debe de estar espacificada la ruta con el send_url como se muestra en el url.rb de controllers -->
    <br>
    <%end%>
</p>

